/* eslint-disable @typescript-eslint/no-unused-vars */
import type { TargettingAlgo } from "./TargettingAlgo"


export const tutorialTargetting : TargettingAlgo = function(ns, network) {
  // These two servers are hardcoded to have the worst growth in the game.
  // fulcrumassets has worse growth but its hacking level isn't relevant for the tutorial.
  const sigmaCosmetics = "sigma-cosmetics"
  const foodnstuff = "foodnstuff"

  return foodnstuff
}
